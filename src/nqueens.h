#pragma once
#include <cstdint>

uint64_t flat_queens(std::size_t n);

uint64_t array_queens(std::size_t n);

uint64_t dlx_queens(std::size_t n);

uint64_t bit_queens(std::size_t n);

uint64_t thread_queens(std::size_t n);

uint64_t super_queens(std::size_t n);
